/**
 * Set to true to temporarily disable auth guards.
 * - All routes become accessible without login
 * - No redirects to login/role-selection
 * - Logout still works (clears local state)
 * Set to false to re-enable full auth.
 */
export const AUTH_DISABLED = true
